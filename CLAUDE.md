check-file-type is a command line program to verify consistency between file extension and contents
It is written in Go to obtain a statically linked binary
We are not writing reusable library code, so errors can be handled by immediate panic
